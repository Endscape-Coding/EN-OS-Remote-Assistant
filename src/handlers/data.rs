pub const STARTRU: &str = r#"
<b>EN-OS Remote Assistant v0.1</b>

<i>Доступные команды:</i>

• <code>/start</code> | <code>/help</code> — <i>Вызвать справку</i>
• <code>/cmd &lt;args&gt;</code> — <i>Запустить bash-команду</i>
• <code>/sudo &lt;passwd&gt;</code> — <i>Установить пароль sudo</i>

────────────────────
<i>Система готова к работе</i>
"#;

pub const STARTEN: &str = r#"
<b>EN-OS Remote Assistant v0.1</b>

<i>Available Commands:</i>

• <code>/start</code> | <code>/help</code> — <i>Show help reference</i>
• <code>/cmd &lt;args&gt;</code> — <i>Execute bash command</i>
• <code>/sudo &lt;passwd&gt;</code> — <i>Set sudo credentials</i>

────────────────────
<i>Ready to work</i>
"#;
